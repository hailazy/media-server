# ================================================================================================
#                                    MEDIA STACK CONFIGURATION
# ================================================================================================
# Copy this file to .env and customize the values for your setup
# All variables marked as (Required) must be configured for the stack to work properly
# Optional variables have sensible defaults but can be customized for optimization

# ================================================================================================
#                                    AIRVPN CREDENTIALS
# ================================================================================================
# AirVPN WireGuard configuration credentials - Required for VPN functionality
# Get these from AirVPN's Configuration Generator at https://airvpn.org/generator/

# Your AirVPN WireGuard private key (Required)
# 1. Log into your AirVPN account at https://airvpn.org/
# 2. Go to Client Area > Config Generator
# 3. Select "WireGuard" as the protocol
# 4. Choose your preferred server(s) and country
# 5. Generate configuration and copy the PrivateKey value
# Example: AIRVPN_WIREGUARD_PRIVATE_KEY=ABC123def456ghi789jkl012mno345pqr678stu901vwx234yz=
AIRVPN_WIREGUARD_PRIVATE_KEY=your_private_key_here

# Your AirVPN WireGuard interface addresses (Required)
# From the same configuration generator, copy the Address values (both IPv4 and IPv6)
# This should include both your assigned IPv4 and IPv6 addresses separated by comma
# Example: AIRVPN_WIREGUARD_ADDRESSES=10.128.0.123/32,fd7d:76ee:e68f:a993::123/128
AIRVPN_WIREGUARD_ADDRESSES=your_addresses_here

# Preferred AirVPN server countries (Optional)
# Comma-separated list of country codes for server selection
# Default: SG (Singapore - optimal for Vietnam users due to low latency)
# Available countries: US, CA, GB, DE, FR, IT, ES, NL, CH, SE, NO, AT, CZ, PL, RO, HU, BG, EE, LV, LT, FI, DK, BE, LU, IE, PT, GR, MT, CY, SI, SK, HR, RS, BA, ME, MK, AL, MD, UA, BY, RU, TR, IL, AE, IN, SG, HK, JP, KR, AU, NZ, BR, AR, MX, CL, ZA
# For Vietnam users, recommended order: SG,HK,JP,KR for best latency
AIRVPN_SERVER_COUNTRIES=SG

# Specific AirVPN server names (Optional)
# Comma-separated list of specific server names to use
# Leave empty to auto-select from preferred countries
# Get server names from: https://airvpn.org/status/ or your config generator
# Example: AIRVPN_SERVER_NAMES=Rigel,Vega,Sirius
AIRVPN_SERVER_NAMES=

# ================================================================================================
#                                   QBITTORRENT CREDENTIALS
# ================================================================================================
# Credentials for qBittorrent web interface - Required for download management
# These will be the login credentials for http://localhost:8080

# qBittorrent web interface username
# Choose a secure username (avoid 'admin' for better security)
QBIT_USER=admin

# qBittorrent web interface password
# Use a strong password - this protects your download client
QBIT_PASS=your_qbit_password_here

# ================================================================================================
#                                 VPN & CONNECTION CONFIGURATION
# ================================================================================================
# Configuration for AirVPN WireGuard connection and VPN optimization

# Enable AirVPN port forwarding for better torrent performance
# AirVPN provides static port forwarding through their client area
# true = Enable port forwarding (recommended for better download speeds)
# false = Disable port forwarding (more secure but potentially slower)
# Note: You must configure port forwarding in your AirVPN client area first
AIRVPN_PORT_FORWARDING=true

# ================================================================================================
#                                    DEBUG & MONITORING
# ================================================================================================
# Enable debug mode for troubleshooting and detailed logging

# Enable debug output for all scripts and enhanced logging
# true = Detailed debug information (helpful for troubleshooting)
# false = Standard logging only (recommended for production)
# Note: Debug mode may expose sensitive information in logs
DEBUG=false

# ================================================================================================
#                                   ADVANCED CONFIGURATION
# ================================================================================================
# Advanced settings - modify only if you understand the implications

# qBittorrent connection settings (used by port update scripts)
# These should match your qBittorrent container configuration
QBIT_HOST=127.0.0.1
QBIT_WEBUI_PORT=8080

# AirVPN connection retry settings
# Maximum retry attempts for VPN connection establishment
# Higher values = more persistent retry attempts
# Lower values = faster failure detection
VPN_RETRY_MAX=5

# Connection timeout for VPN establishment (seconds)
# Higher values = more patience for slow connections
# Lower values = faster detection of connection problems
VPN_CONNECT_TIMEOUT=30

# ================================================================================================
#                                    PODMAN CONFIGURATION
# ================================================================================================
# Podman and container runtime settings

# Timezone for all containers (use your local timezone)
# Format: Continent/City (e.g., America/New_York, Europe/London, Asia/Tokyo)
# For Vietnam users: Asia/Ho_Chi_Minh or Asia/Saigon
# Default: UTC (recommended unless you need local time in logs)
TZ=UTC

# User ID and Group ID for file permissions
# Use 'id' command to find your user ID
# This ensures proper file ownership for media files
PUID=1000
PGID=1000

# ================================================================================================
#                                      MEDIA PATHS
# ================================================================================================
# Local paths for media storage - customize for your system

# Base path for all media storage
# This should be a location with plenty of free space
MEDIA_ROOT=/media/Storage

# Download directory for qBittorrent
# This is where torrents will be downloaded before processing
DOWNLOADS_PATH=${MEDIA_ROOT}/downloads

# Movies directory for Radarr
# Processed movie files will be organized here
MOVIES_PATH=${MEDIA_ROOT}/movies

# TV shows directory for Sonarr
# Processed TV show files will be organized here
TV_PATH=${MEDIA_ROOT}/tv-shows

# ================================================================================================
#                                   CONFIGURATION NOTES
# ================================================================================================
#
# AIRVPN SETUP GUIDE:
# 1. Create account at https://airvpn.org/
# 2. Purchase a subscription plan
# 3. Access Config Generator: Client Area > Config Generator
# 4. Select WireGuard protocol
# 5. Choose servers (Singapore recommended for Vietnam users)
# 6. Generate config and extract PrivateKey and Address values
# 7. Optional: Configure port forwarding in Client Area > Forwarded Ports
#
# SECURITY BEST PRACTICES:
# - Use strong, unique passwords for all services
# - Consider using a password manager to generate secure credentials
# - Regularly update passwords and review access logs
# - Keep your AirVPN account secure with 2FA (highly recommended)
# - Never share your WireGuard private key
#
# PERFORMANCE OPTIMIZATION FOR VIETNAM USERS:
# - Singapore (SG) servers typically provide best latency from Vietnam
# - Hong Kong (HK) and Japan (JP) are good alternatives
# - Enable port forwarding for optimal torrent performance
# - Monitor DEBUG logs initially, then disable for production use
#
# TROUBLESHOOTING:
# - Enable DEBUG=true if experiencing connection issues
# - Check logs with: podman-compose logs -f SERVICE_NAME
# - Verify AirVPN credentials are correct and account is active
# - Test WireGuard config manually before using in containers
# - Ensure AirVPN account has active subscription
# - Check AirVPN status page: https://airvpn.org/status/
# - Verify port forwarding is properly configured in AirVPN client area
# - Ensure sufficient disk space in MEDIA_ROOT directory
#
# AIRVPN SPECIFIC NOTES:
# - AirVPN uses static IP assignments for each account
# - Port forwarding ports are assigned per account, not per session
# - Multiple simultaneous connections allowed (check your plan limits)
# - WireGuard configs don't expire but can be regenerated if needed
# - For best results in Vietnam, use servers: SG > HK > JP > KR
#
# For more help, see the troubleshooting section in README.md
# AirVPN documentation: https://airvpn.org/faq/
# ================================================================================================

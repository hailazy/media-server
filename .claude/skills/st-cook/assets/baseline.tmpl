## embodied
# «NAME» — per-shot baseline (persona; identity lives in persona_descriptions["«NAME» (Persona).png"])
# body (not in the permanent face block): «BODY_TAGS»
# on duty: «ON_DUTY_TAGS»
# at home: «AT_HOME_TAGS»
# mature lock-in (positive): «MATURE_LOCK_IN»
# negative (always): «NEGATIVE_ALWAYS»

## creature-only
# «CREATURE_NOUN_CAP» — per-shot baseline (narrator card; character_prompts deliberately EMPTY so nothing is painted onto the host)
# creature-only render: «CREATURE_TAGS»
# creature-only negative (paste per shot only): «CREATURE_NEGATIVE»
# host-in-frame shots: «HOST_FRAME_RULE»

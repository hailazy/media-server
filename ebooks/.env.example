# Calibre library on the Data drive (separated from OneDrive mount).
# CWA mounts this read-write into /calibre-library.
# Daily backup to OneDrive cloud via systemd timer (calibre-sync.timer).
# Spaces are fine — quoting is handled in compose.yml.
CALIBRE_LIBRARY_PATH=/home/haint/Data/Calibre Library

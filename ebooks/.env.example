# Existing Calibre library on the host (OneDriveGUI keeps it synced to cloud).
# CWA mounts this read-write into /calibre-library.
# Spaces are fine — quoting is handled in compose.yml.
CALIBRE_LIBRARY_PATH=/home/haint/OneDrive/dev/Calibre Library
